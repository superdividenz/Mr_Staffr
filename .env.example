# Database (required). Use Neon, Supabase, or local Postgres.
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# NextAuth (required for auth)
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Optional: platform wallet for fees (ERC-20 address)
# PAYMENT_WALLET_ADDRESS="0x..."
# PLATFORM_FEE_PERCENT=10

# User wallet addresses (ERC-20, 0x + 40 hex) are stored in DB for receiving USDC/stablecoins.
